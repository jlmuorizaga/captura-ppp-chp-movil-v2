import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RelacionOrillaSucursalPpalPage } from './relacion-orilla-sucursal-ppal.page';

describe('RelacionOrillaSucursalPpalPage', () => {
  let component: RelacionOrillaSucursalPpalPage;
  let fixture: ComponentFixture<RelacionOrillaSucursalPpalPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RelacionOrillaSucursalPpalPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
