import { ComponentFixture, TestBed } from '@angular/core/testing';
import { OrillasPpalPage } from './orillas-ppal.page';

describe('OrillasPpalPage', () => {
  let component: OrillasPpalPage;
  let fixture: ComponentFixture<OrillasPpalPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(OrillasPpalPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
