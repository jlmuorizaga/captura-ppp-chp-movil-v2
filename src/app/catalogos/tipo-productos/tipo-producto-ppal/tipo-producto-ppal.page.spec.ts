import { ComponentFixture, TestBed } from '@angular/core/testing';
import { TipoProductoPpalPage } from './tipo-producto-ppal.page';

describe('TipoProductoPpalPage', () => {
  let component: TipoProductoPpalPage;
  let fixture: ComponentFixture<TipoProductoPpalPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(TipoProductoPpalPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
