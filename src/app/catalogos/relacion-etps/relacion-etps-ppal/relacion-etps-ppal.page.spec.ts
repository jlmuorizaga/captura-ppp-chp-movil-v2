import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RelacionEtpsPpalPage } from './relacion-etps-ppal.page';

describe('RelacionEtpsPpalPage', () => {
  let component: RelacionEtpsPpalPage;
  let fixture: ComponentFixture<RelacionEtpsPpalPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RelacionEtpsPpalPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
