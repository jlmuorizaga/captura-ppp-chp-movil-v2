import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RelacionPromocionEspecialSucursalPpalPage } from './relacion-promocion-especial-sucursal-ppal.page';

describe('RelacionPromocionEspecialSucursalPpalPage', () => {
  let component: RelacionPromocionEspecialSucursalPpalPage;
  let fixture: ComponentFixture<RelacionPromocionEspecialSucursalPpalPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RelacionPromocionEspecialSucursalPpalPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
