import { ComponentFixture, TestBed } from '@angular/core/testing';
import { EditarRelacionPromocionEspecialSucursalPage } from './editar-relacion-promocion-especial-sucursal.page';

describe('EditarRelacionPromocionEspecialSucursalPage', () => {
  let component: EditarRelacionPromocionEspecialSucursalPage;
  let fixture: ComponentFixture<EditarRelacionPromocionEspecialSucursalPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(EditarRelacionPromocionEspecialSucursalPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
