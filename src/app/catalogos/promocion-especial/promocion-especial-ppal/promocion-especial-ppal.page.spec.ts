import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PromocionEspecialPpalPage } from './promocion-especial-ppal.page';

describe('PromocionEspecialPpalPage', () => {
  let component: PromocionEspecialPpalPage;
  let fixture: ComponentFixture<PromocionEspecialPpalPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(PromocionEspecialPpalPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
